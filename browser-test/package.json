{
  "name": "@tach/browser-test",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "description": "Private headless-browser and WebGPU harness for Tach-generated modules",
  "type": "module",
  "imports": {
    "#kernels": "./generated/index.js"
  },
  "scripts": {
    "build": "deno run -A --node-modules-dir=manual scripts/build.ts",
    "check": "deno lint scripts src test.ts && deno check --node-modules-dir=manual scripts/build.ts src/main.ts test.ts",
    "test": "npm run build && deno run -A test.ts"
  },
  "dependencies": {
    "@depths/tach": "0.1.2"
  }
}
