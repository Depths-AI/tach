export compute reduceLanes[i](data: buffer<u32[]>) {
  const base = i * 4;
  let sum = 0;
  for (let lane = 0; lane < 4; lane++) {
    sum += data[base + lane];
  }
  if (i < data.length) {
    data[i] = sum;
  }
}
