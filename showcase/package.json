{
  "name": "@tach/showcase",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "description": "Large dual-host GPU workload showcase for Tach",
  "type": "module",
  "imports": {
    "#kernels": "./build/index.js"
  },
  "scripts": {
    "build": "deno run -A --node-modules-dir=manual scripts/build.ts",
    "check": "deno lint scripts src benchmark.ts && deno check --node-modules-dir=manual scripts/build.ts src/*.ts benchmark.ts",
    "benchmark": "npm run build && deno run -A --node-modules-dir=manual benchmark.ts",
    "test": "npm run benchmark"
  },
  "dependencies": {
    "@depths/tach": "0.2.0"
  }
}
