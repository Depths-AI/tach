{
  "name": "tach-workspace",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "workspaces": [
    "tach-ts",
    "browser-test",
    "showcase-ts"
  ],
  "scripts": {
    "compiler": "go build -o dist/ .",
    "build": "npm run compiler && npm run build -w @depths/tach && npm run build -w @tach/showcase-ts",
    "check": "npm run compiler && npm run build -w @depths/tach && npm run check -w @tach/showcase-ts",
    "test:spirv": "go test -count=1 -v ./spirv-test",
    "test": "npm run compiler && npm test -w @depths/tach && npm test -w @tach/browser-test && npm test -w @tach/showcase-ts"
  },
  "engines": {
    "node": ">=22"
  }
}
